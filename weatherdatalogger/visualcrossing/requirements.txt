paho-mqtt
# 1.0.2+ declares aiohttp as its own dependency (earlier versions imported it
# unconditionally in api.py without declaring it, leaving `pip install` with
# a broken import) and exposes the full field set this service reads
# (snow, precipitation_type, solar_energy, severe_risk, sunrise/sunset,
# moon_phase, etc.) — see visualcrossing_datalogger.py's payload builders.
pyVisualCrossing>=1.0.2
